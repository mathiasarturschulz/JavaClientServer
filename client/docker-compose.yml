version: '3.3'
services:
    java-client:
        image: 'openjdk:12'
        container_name: java-client-container
        volumes: 
            - ./app:/root/project
        command: bash -c "cd /root/project && javac Main.java && java Main"
        # stdin_open corresponds to -i in docker run and the tty to -t
        stdin_open: true
        tty: true
        # docker-compose run --rm java-client
